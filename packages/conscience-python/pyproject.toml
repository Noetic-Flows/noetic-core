[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "noetic-conscience"
version = "0.1.0"
description = "Axiological Safety Engine for Noetic"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "json-logic-qubit",
    "cryptography"
]

[tool.setuptools.packages.find]
where = ["."]
